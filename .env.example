# MCP SHIELD — Environment Configuration
# Optional: The prototype includes a deterministic local AI engine and local database
# so it operates 100% reliably in DEMO MODE without any external API keys.

# Optional AI Providers (Gemini / OpenAI)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Server Configuration
PORT=3000
NODE_ENV=development
